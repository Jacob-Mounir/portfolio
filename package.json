{
  "name": "portfolio",
  "version": "0.0.1",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@github.com:Jacob-Mounir/portfolio.git"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && git checkout gh-pages && rm -rf $(ls -A | grep -v '.git') && mv dist/* . && rm -rf dist && git add . && git commit -m 'deploy: latest build' && git push origin gh-pages && git checkout main"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.0.3",
    "@astrojs/tailwind": "^5.0.3",
    "@tailwindcss/typography": "^0.5.10",
    "@types/three": "^0.169.0",
    "astro": "^4.0.4",
    "daisyui": "^4.12.10"
  },
  "dependencies": {
    "@astrojs/react": "^3.6.2",
    "@astrolib/seo": "^1.0.0-beta.5",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "axios": "^1.7.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "solid-js": "^1.8.21",
    "three": "^0.170.0",
    "usestate": "^1.1.3"
  }
}
